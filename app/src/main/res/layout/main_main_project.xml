<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nfibet3Liner"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/nfibet3"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".main_main_project">

    <soup.neumorphism.NeumorphTextView
        android:id="@+id/nfibet3txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="350dp"
        android:text="@string/nfibet3_app_name"
        android:textColor="@color/nfibet3_back"
        android:textSize="28sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:neumorph_shadowColorDark="@color/nfibet3_dark"
        app:neumorph_shadowColorLight="@color/nfibet3_light" />

    <WebView
        android:id="@+id/nfibet3WebView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:focusable="true"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ProgressBar
        android:id="@+id/nfibet3progressBar"
        android:layout_width="102dp"
        android:layout_height="76dp"
        android:layout_marginTop="230dp"
        android:indeterminateDrawable="@drawable/nfibet3_progressbar"
        android:indeterminateOnly="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/nfibet3txt" />
</androidx.constraintlayout.widget.ConstraintLayout>